applications:
  - name: express-proxy-app  # Change to your desired app name
    memory: 256M
    instances: 1
    buildpacks:
      - nodejs_buildpack
    path: .  # Path to your application code
    command: npm start
